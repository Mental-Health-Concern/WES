<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>EOC_Required_with_Outcome</fullName>
    <active>true</active>
    <description>Must have Client and Case to add outcome to this appointment type</description>
    <errorConditionFormula>AND(
NOT( ISBLANK(text(Outcome__c) ) ),
ISBLANK( Case__c ),
ISBLANK( Contact__c )
)</errorConditionFormula>
    <errorMessage>Must have Client and Case to add outcome to this appointment type</errorMessage>
</ValidationRule>
